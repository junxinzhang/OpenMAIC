# 接入验证记录

日期：2026-09-20 至 2026-09-21。环境：本地独立 PostgreSQL、真实 Resend 测试收件地址、Stripe 沙盒。

## 已实际完成

- 浏览器打开登录页；Resend 返回邮件已送达；使用邮件里的真实验证码完成账户登录。
- 浏览器从账户页进入 Stripe Checkout，用官方测试卡完成 Edu 测试订阅。
- Stripe CLI 转发的真实 `invoice.paid`、`customer.subscription.created`、
  `checkout.session.completed` 全部收到 HTTP 200，账户显示 1,000 积分。
- 重放同一付款事件两次仍只有一张账单、一次 1,000 积分发放。
- 浏览器进入 Stripe 订阅管理并设置期末取消；Edu 显示仍有效且期末取消，当前积分保留。
- 使用现有模型服务实际生成两页课程 `JHvbBLUbvh`（任务 `lxtKzjydCN`），任务成功。
- 积分从 1,000 变为 980，预留归零；浏览器打开课程、查看内容并翻到第二页。
- 第二账户及游客读取该私有课程返回 404；第二账户查询任务返回 404。
- 实际创建学习记录返回 201，重新读取返回 200；伪造另一学习者身份返回 403，私有响应禁止缓存。
- 无积分账户触发生成返回 402；重复提交同一生成请求返回 409，没有新任务或二次扣除。

## 自动验证

- 核心账户、支付、维护、权限、持久化及服务器测试：853 通过，19 个原有跳过。
- Agent 全目录回归：1,035 通过，10 个跳过；唯一并发负载超时用例单独运行 19 项通过。
- 账户缓存及新账户设置另行验证，修复图片/视频提供方默认配置缺失；相关 83 项通过。
- 完整 TypeScript 检查通过；生产构建通过。
- 后续增补的私有响应缓存、无旧 token 存取及后台材料费用检查均有定向测试。

## 尚未声称完成的外部验证

- Google 的令牌签名、状态绑定、一次性使用及账户关联已自动测试；给定生产回调地址已接入代码；浏览器访问 Google 已正常进入登录页，没有回调地址不匹配错误。
  真实 Google 用户完成生产授权，仍需新版本在该回调地址上线后验证。
- 没有真实扣款；测试套餐为 USD 9/月、1,000 测试积分，不代表正式商业定价。
- 没有替换现有线上版本或迁移生产旧资料；生产启用前须落实历史归属及正式定价。

所有测试账户与付款对象均为 Edu 独立测试数据；没有修改 Cowork 用户、订阅或积分。

## 2026-09-21 上线前修复

- 测试与正式客户、付款会话、余额、账单、流水按环境隔离。旧的未标记付款数据只属于测试环境；切换环境不能消费测试积分。已开始任务仍按原积分批次结算。
- 退款或争议进入核对时，暂停该 Edu 客户名下 Edu 订阅的后续收款并安排期末取消。冻结积分保留原有效期；迟到付款记录不会发放可消费积分，也不会伪造积分已到期。核对期间不接受新订阅。
- 在发起外部付款前保存不可变的套餐权益。删除或修改当前套餐列表不影响已购订阅；同一价格不能改发不同积分，必须另建价格。
- 覆盖上述问题的数据库测试已通过；账户、积分、持久化、后台任务和访问隔离回归 1,893 通过，29 跳过。另补 3 项 Checkout 测试通过。
- 用户指定 4 门旧课程归属 `junxinzhang1616@gmail.com`。上线前备份已在独立验证库完整恢复，含 4 门课程、52 页内容；归属迁移先在验证库执行。
